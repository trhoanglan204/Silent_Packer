#ifndef PACK_PE_H
#define PACK_PE_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int pack_pe(char* file, char* file_data, size_t file_data_size, char* output);

#endif //PACK_PE_H